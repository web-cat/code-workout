%h1 Editing Course

= render 'form'

.form-horizontal
  .controls
    = link_to @course, class: 'btn' do
      %i.icon-zoom-in
      Show
    = link_to courses_path, class: 'btn' do
      %i.icon-reply
      Back
