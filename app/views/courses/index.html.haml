%h1 Courses

%table.table.table-striped.table-bordered
  %thead
    %tr
      %th Name
      %th Number
      %th Organization
      %th Url part
      %th Actions

  %tbody
    - @courses.each do |course|
      %tr
        %td= course.name
        %td= course.number
        %td= course.organization_id
        %td= course.url_part
        %td
          = button_link 'Show', course, btn: 'mini'
          = button_link 'Edit', edit_course_path(course), btn: 'mini'
          = button_link 'Destroy', course, method: :delete, btn: 'mini',  |
            data: { confirm: 'Are you sure?' }                            |

= button_link 'New Course', new_course_path
