.nested-fields
  = f.inputs do
    .row-fluid.darker-bkg
      .row-fluid
        .span6
          = f.input :tag_name, :as => :text,
            placeholder: "Enter tag",
            :input_html => { :class => 'span10', :rows =>1 }
        .span4
          = link_to_remove_association "Remove tag...", f