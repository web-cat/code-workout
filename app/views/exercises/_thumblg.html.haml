.well.well-small.span6
  .row-fluid
    %p.teaser-title.span10
      = link_to x.title, practice_path(:id => x[:id])
    .span2
      = difficulty_image(x.difficulty.to_i)
  .row-fluid
    - x.tags.each do |t|
      %a.tag{ href: '#' }= t[:tag_name]
  .row-fluid
    = x.teaser_text