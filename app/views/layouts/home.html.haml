= inside_layout 'header' do
  = render partial: 'layouts/navbar'
  .below-nav.content.full-height.container-fluid
    = render partial: 'layouts/flash'
    = yield
    - if !embedded_lti_launch?
      = render partial: 'layouts/footer'
