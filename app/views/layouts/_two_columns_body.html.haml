.row.max-height#main{class: "#{controller_name}-#{action_name}"}
  .col-md-3.scrollable.max-height.sidebar
    .inner.max-height
      .below-nav#sidebar
        = render partial: 'layouts/sidebar'
  .col-md-9.scrollable.max-height.content
    .inner.max-height
      .below-nav
        = render partial: 'layouts/flash'
        = yield
