= inside_layout do
  .row.max-height#main
    .col-md-9.scrollable.max-height.leftcol
      .below-nav
        = render partial: 'layouts/flash'
        = yield

    .col-md-3.scrollable.max-height.rightcol
      .below-nav

        = render partial: 'layouts/sidebar'
