= inside_layout do
  .row-fluid.max-height#main
    .span8.scrollable.max-height.leftcol
      .below-nav
        = render partial: 'layouts/flash'
        = yield

    .span4.scrollable.max-height.rightcol
      .below-nav

        = render partial: 'layouts/sidebar'
