= inside_layout do
  .row.full-height#main
    .col-md-3.scrollable.full-height.sidebar
      .below-nav
        = render partial: 'layouts/sidebar'
    .col-md-6.scrollable.full-height.content
      .below-nav
        = render partial: 'layouts/flash'
        = yield
    .col-md-3.scrollable.full-height.content
      .below-nav
        = render partial: 'exercises/feedback'
