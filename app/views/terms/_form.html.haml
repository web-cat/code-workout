= semantic_form_for @term, html: { class: "form-horizontal" } do |f|
  = f.semantic_errors
  = f.inputs
  = f.actions do
    = f.action :submit, as: :button, button_html: { class: 'btn btn-primary' }
