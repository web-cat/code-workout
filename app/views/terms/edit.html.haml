%h1 Editing Term

= render 'form'

.form-horizontal
  .controls
    = link_to @term, class: 'btn' do
      %i.icon-zoom-in
      Show
    = link_to terms_path, class: 'btn' do
      %i.icon-reply
      Back
