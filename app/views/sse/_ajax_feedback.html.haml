- show_feedback = !@attempt.workout_score || @attempt.workout_score.show_feedback?
- show_score = !@attempt.workout_score || @attempt.workout_score.show_score?
- @workout ||= @attempt.workout_score.andand.workout
- if @workout && show_score
  - pts = @max_points || (@workout.exercise_workouts.loaded? ? @workout.exercise_workouts.find { |ew| ew.exercise_id == @exercise.id }&.points : @workout.exercise_workouts.find_by(exercise: @exercise)&.points) || @exercise.andand.experience
  - score = @attempt.score
  - score_pct = (pts && pts > 0) ? score * 100.0 / pts : 0
  .summary
    .radial-progress{ 'data-progress' => score_pct.round() }
      .circle
        .mask.full
          .fill
        .mask.half
          .fill
          .fill.fix
        .shadow
      .inset
        .score
          = score.round(2).to_s
          %span.outof>
            = "/ "  + pts.to_s

- if @exercise_version.is_mcq?
  - if show_score
    - if @attempt.earned_full_points?
      %p Your choice is correct!
    - elsif @attempt.score > 0
      %p Your choice is partially correct.
    - else
      %p Your choice is incorrect.
    -#= Choice.find(attempt.answer.to_i).feedback
    - @attempt.prompt_answers.first.specific.choices.each do |choice|
      %p= choice.feedback
  - else
    Your answer has been saved.
- elsif @exercise_version.is_parsons? && !@exercise_version.is_execution_graded?
  - if show_score
    - if @attempt.earned_full_points?
      %p#parsons-feedback-message Your arrangement is correct!
    - else
      %p#parsons-feedback-message Your arrangement is incorrect.
  - else
    %p Your answer has been saved.
- elsif @exercise_version.is_coding? || @exercise_version.is_parsons?
  - answer = @attempt.prompt_answers.first.specific
  - if !answer.error.blank?
    %p Your answer could not be processed because it contains errors:
    .alert.alert-danger{ role: 'alert' }
      - answer.error.lines.each_with_index do |line, i|
        - if i > 0
          %br
        = line
  - elsif show_feedback
    %table.table.table-striped.feedback
      %thead
        %tr
          %th{ style: 'text-align: center;' } Result
          %th Behavior
      %tbody
        - has_hidden = false
        - hidden_pass = true
        - answer.test_case_results().each do |tcr|
          - if tcr.test_case.hidden?
            - has_hidden = true
            - hidden_pass = hidden_pass && tcr.pass
          - else
            %tr
              - if tcr.pass
                %td.pass
                  %i.fa.fa-check-square
              - else
                %td.fail
                  %i.fa.fa-minus-square
              %td
                = tcr.display_description
                - if !tcr.pass
                  %br
                  .out= tcr.execution_feedback
        - if has_hidden
          %tr
            - if show_score
              - if hidden_pass
                %td.pass
                  %i.fa.fa-check-square
              - else
                %td.fail
                  %i.fa.fa-minus-square
              %td hidden test(s)
            - else
              %td.unknown
                %i.fa.fa-question-circle
              %td hidden test(s) (results not shown)
  - else
    - examples = @exercise_version.prompts.first.specific.examples
    %table.table.table-striped.feedback
      %thead
        %tr
          %th{ style: 'text-align: center;' } Result
          %th Behavior
      %tbody
        - if examples.any?
          - answer.test_case_results().each do |tcr|
            - if tcr.test_case.is_example?
              %tr
                - if tcr.pass
                  %td.pass
                    %i.fa.fa-check-square
                - else
                  %td.fail
                    %i.fa.fa-minus-square
                %td
                  - description = tcr.display_description
                  = description
                  - if !tcr.pass
                    - if !tcr.test_case.hidden?
                      %br
                      .out= tcr.execution_feedback
        %tr
          %td.unknown
            %i.fa.fa-question-circle
          %td hidden test(s) (results not shown)

-#= render partial: 'workout_scores/time_remaining',
  locals: { workout_score: @attempt.workout_score }
