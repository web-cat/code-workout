- if @course_offering.students.any?
  $('tbody', '.offering-fields').append("#{escape_javascript(render partial: 'workouts/student_extension_fields', locals: { students: @course_offering.students })}")
