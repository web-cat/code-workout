%h1 Listing Course Offerings

%table.table.table-striped.table-bordered
  %thead
    %tr
      %th Course
      %th Term
      %th Name
      %th Label
      %th URL
      %th Self enrollment allowed
      %th Actions

  %tbody
    - @course_offerings.each do |course_offering|
      %tr
        %td= course_offering.course_id
        %td= course_offering.term_id
        %td= course_offering.name
        %td= course_offering.label
        %td= course_offering.url
        %td= course_offering.self_enrollment_allowed
        %td
          = link_to 'Show', course_offering, class: 'btn btn-mini'
          = link_to 'Edit', edit_course_offering_path(course_offering), class: 'btn btn-mini'
          = link_to 'Destroy', course_offering, method: :delete,         |
            data: { confirm: 'Are you sure?' }, class: 'btn btn-mini'    |

= link_to 'New Course offering', new_course_offering_path, class: 'btn'
