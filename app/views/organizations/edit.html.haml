%h1 Editing Organization

= render 'form'

.form-horizontal
  .controls
    = link_to @organization, class: 'btn' do
      %i.icon-zoom-in
      Show
    = link_to organizations_path, class: 'btn' do
      %i.icon-reply
      Back
